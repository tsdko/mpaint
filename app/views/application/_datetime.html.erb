<%
  main = datetime
  sub = time_ago_in_words(datetime) + " ago"
  main, sub = sub, main if local_assigns[:relative]
%>
<time
  datetime="<%= datetime.rfc3339 %>"
  <%= "data-is-relative=\"true\"" if local_assigns[:relative] %>
  title="<%= sub %>">
    <%= main %>
</time>
